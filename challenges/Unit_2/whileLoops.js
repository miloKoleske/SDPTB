























console.log(`while loop is finished running, final value ${}`)



let cartMsg = '';
let z = 0; // zero items in cart

while ( z < 5 ) {
    cartMsg = `You have ${z} item(s) in your cart!`;
    console.log(cartMsg);
}

console.log(`You have ${z} items in your cart, want to checkout now?`);
















let structure = city[0];
// console.log(structure); // building

let pos = 0;
// console.log(city[pos]); // building

console.log("Welcome to the Adventures of Spider Man!! Looks like there's a thief on the loose!");

while ( structure != "empty street") {
    console.log(`Spider Man swings from the ${structure} in pursuit of the thief!`);
    pos++;
    structure = city[pos];
    console.log(`spiderman lands on the ${structure}`);
}


                                                                        counter < 0             console.log()
// 1) Write a program to keep asking for a number until you enter a negative number. At the end, print the sum of all entered numbers.

counter = 5

while (counter > 0) {
    console.log(counter)
    counter--
}
console.log(counter)

total = 0;
enteredNum = 5;
while (enteredNum > 0){
    total--= enteredNum
    enteredNum = (`enter number:`)
}
console.log(total)


// Function `printArray()` prints all the elements of a 2D array using nested for loops.

function printArray = {
    for
}
